REP 10 — GATED CLOSED LOOP

Prediction before execution

I expect the loop to diagnose the disk pressure automatically, stop before the
irreversible journal vacuum, and return exit 2 without approval. After I supply
the exact token, I expect one simulated action followed by a fresh check of the
free-space signal and resolution only when it exceeds 25 percent.

Blocked run

[PASTE output/rep10-gated-blocked.txt.]

Approved run

[PASTE output/rep10-gated-approved.txt.]

Reflection draft — paraphrase this

VERIFY checks node_filesystem_avail_bytes because success means the underlying
disk-pressure condition improved, not merely that a command returned zero. A
vacuum command could exit successfully while freeing too little space, acting
on the wrong journal, or leaving another directory to consume the filesystem;
an exit-code-only loop could then close an unresolved incident.
